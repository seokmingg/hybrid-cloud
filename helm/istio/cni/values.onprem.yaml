cni:
  enabled: true
  cniBinDir: /opt/cni/bin
  cniConfDir: /etc/cni/net.d
  chained: false
  logLevel: info
  excludeNamespaces:
    - istio-system
    - kube-system
  namespace: kube-system

global:
  istioNamespace: istio-system