singleBinary:
  enabled: false

loki:
  auth_enabled: false
  commonConfig:
    replication_factor: 1
  storage:
    type: filesystem
    filesystem:
      directory: /var/loki
  schemaConfig:
    configs:
      - from: "2024-01-01"
        store: boltdb-shipper
        object_store: filesystem
        schema: v11
        index:
          prefix: index_
          period: 24h

  persistence:
    enabled: true
    existingClaim: loki-shared
    mountPath: /var/loki
    storageClassName: nfs-storage
    size: 20Gi

  # Override default volume mounts
  volumes: []
  volumeMounts: []

backend:
  enabled: true

compactor:
  enabled: true
  persistence:
    enabled: false
  extraVolumes: []
  extraVolumeMounts: []

distributor:
  enabled: true
  extraVolumes: []
  extraVolumeMounts: []

ingester:
  replicas: 1
  persistence:
    enabled: false
  wal:
    enabled: true
    dir: /var/loki/wal
  extraVolumes: []
  extraVolumeMounts: []

querier:
  replicas: 1
  persistence:
    enabled: false
  extraVolumes: []
  extraVolumeMounts: []

queryFrontend:
  replicas: 1
  extraVolumes: []
  extraVolumeMounts: []

read:
  replicas: 1
  persistence:
    enabled: false
  extraVolumes: []
  extraVolumeMounts: []

write:
  replicas: 1
  persistence:
    enabled: false
  extraVolumes: []
  extraVolumeMounts: []

gateway:
  enabled: true

test:
  enabled: false